---
title: "Feature: Risk-Adaptive Decision Gate (RADG)"
date: 2026-08-06
tags: [feature, radg, physical-risk, phase6, nodes, core]
status: active
---

# Feature: Risk-Adaptive Decision Gate (RADG)

## 1. Architecture Placement
**Phase 6: RADG** | [[Architecture_v5]]

The Risk-Adaptive Decision Gate (RADG) is the core contribution of Architecture V5. It evaluates the physical feasibility (QoT) of candidate paths generated by the Symbolic Solver. It operates *after* the [[semantic_gate]] has verified intent clarity, focusing entirely on physical risk.

## 2. Overview
The RADG maps the QoT evaluation results from Phase 5 to a final action space: `{approve, replan}`. The `clarify` action is handled upstream by the Semantic Gate, ensuring a clean separation of concerns.

## 3. How it Works
The decision logic is implemented in `src/core/radg.py` as a deterministic function:
- **`approve`**: If *at least one* candidate path passes QoT validation (i.e., `feasible == True`).
- **`replan`**: If *all* candidate paths fail QoT validation (or if no paths were generated).

The LangGraph node `radg_node.py` executes this decision:
- On **approve**, the graph routes to the [[plan_synthesizer]] to finalize the optimal route.
- On **replan**, the graph triggers a HITL `interrupt()` to notify the operator that physical constraints cannot be met, suggesting they relax GSNR requirements or allow longer routes, before looping back to the PDDL parser.

## 4. Associated Files
- **Core Logic**: [src/core/radg.py](file:///home/felipeab/MultiAgentON/src/core/radg.py) — `evaluate_radg()`
- **LangGraph Node**: [src/nodes/radg_node.py](file:///home/felipeab/MultiAgentON/src/nodes/radg_node.py) — `radg_node()`, `radg_route()`
- **Tests**: [tests/unit/test_radg.py](file:///home/felipeab/MultiAgentON/tests/unit/test_radg.py)

## 5. Inputs / Outputs
- **Input (State)**: `qot_results` (list of dicts from Phase 5).
- **Output (State)**: `radg_decision` (str: `"approve"` or `"replan"`), `messages` (AIMessage with trace).

## 6. How to Test
```bash
uv run pytest tests/unit/test_radg.py -v
```

## 7. Cross-References
- [[Architecture_v5]] — Phase 6 description
- [[architecture/features/qot_tool]] — Feeds the `qot_results` to this gate
- [[architecture/features/plan_synthesizer]] — Target node on approval
- [[architecture/features/pipeline_graph]] — Contains the conditional routing logic
